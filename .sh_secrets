eval $(op signin --account my)

export HONEYCOMB_WRITEKEY=$(op item get --vault EnvVars --fields password HONEYCOMB_WRITEKEY)
export HOMEBREW_GITHUB_API_TOKEN=$(op item get --vault EnvVars --fields password HOMEBREW_GITHUB_API_TOKEN)
